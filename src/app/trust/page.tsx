import { CorporatePageView, pageMetadata } from "@/lib/corporate-pages"
export const metadata = pageMetadata("trust", "/trust")
export default function Page() { return <CorporatePageView pageKey="trust" /> }
